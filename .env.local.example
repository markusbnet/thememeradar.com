# Reddit API Credentials (for fetching posts/comments, NOT for user login)
# Create an OAuth app at: https://www.reddit.com/prefs/apps
# Choose "script" type for backend access
# Note: This is separate from user authentication (users login with email/password)
REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here
REDDIT_USER_AGENT=MemeRadar/1.0 by YourRedditUsername

# DynamoDB Configuration
# Local Development (using DynamoDB Local via Docker)
# Note: Using port 8080 (existing container from memoney project)
DYNAMODB_ENDPOINT=http://localhost:8080
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local

# Production (comment out for local development)
# DYNAMODB_ENDPOINT=                    # Leave empty for AWS DynamoDB
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# Authentication
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_generate_with_openssl
SESSION_COOKIE_NAME=meme_radar_session
SESSION_EXPIRATION_DAYS=7

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Feature Flags (optional)
ENABLE_WATCHLISTS=false
ENABLE_ALERTS=false
